Switch to Zenmux Account 2 (Claude Opus). Run this command:

```bash
bash ~/.claude/zenmux-switch.sh 2
```

Then confirm: "Switched to Zenmux Account 2 (anthropic/claude-opus-4.6). Restart Claude Code session to apply."
