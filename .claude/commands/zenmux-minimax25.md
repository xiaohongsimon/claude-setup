Switch to 百炼 MiniMax-2.5. Run this command:

```bash
bash ~/.claude/zenmux-switch.sh minimax25
```

Then confirm: "Switched to 百炼 MiniMax-2.5 (bailian/minimax-2.5). Restart Claude Code session to apply."
