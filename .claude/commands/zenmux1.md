Switch to Zenmux Account 1 (Claude Opus). Run this command:

```bash
bash ~/.claude/zenmux-switch.sh 1
```

Then confirm: "Switched to Zenmux Account 1 (anthropic/claude-opus-4.6). Restart Claude Code session to apply."
